{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - La boutique française{% endblock %}

{% block content %}
    <h1>Modifier mon mot de passe compte</h1>
    {# {{ dump(app.user.firstname) }} #}
    <a href="{{path('app_account')}}">Retourner à mon compte</a>
    <hr>
    {% if notification %}
        <div class="alert alert-info">{{notification}}</div>
    {% endif %}
    {{form(form)}}
    <hr>
{% endblock %}
